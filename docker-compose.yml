version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      SECRET_KEY: change-me-in-production
      DATABASE_URL: sqlite+aiosqlite:///./data/pulsecheck.db
    volumes:
      - ./data:/app/data
